# Compile-dependency packages — large third-party .u inputs dropped in to satisfy
# the compiler (stock UT99 + Nexgen + IACE). These are NOT part of this plugin and
# should not be version-controlled here.
*.u
# ...except this plugin's own compiled output, which we do keep.
!NexgenACEExt14_2_fmj.u

# Build scratch
*.log

# Investigation data / send-out bundle (not part of the plugin)
Nexgen_hasright_kicks.txt
ACE-bug-report-bundle.zip

# Copied-in ACE/Nexgen reference files (server artifacts, not plugin source)
ACEFileList.txt
ACETweakList.txt
ACEv14e_S.dll
Nexgen.ini
